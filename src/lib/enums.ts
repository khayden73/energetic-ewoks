
export enum ListTypes {
    ORDERED = 'ORDERED',
    UNORDERED = 'UNORDERED',
}

export enum TileVariants {
    CONTENT = 'CONTENT',
    PRODUCT = 'PRODUCT',
    PHOTO = 'PHOTO',
}

export enum TileOrientation {
    COLUMN = "COLUMN",
    ROW = "ROW",
}